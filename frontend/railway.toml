[build]
builder = "nixpacks"

[deploy]
startCommand = "npm start"
# Railway injects $PORT — next start reads it via package.json
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
